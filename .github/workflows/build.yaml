name: build

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'

  pull_request:
    branches:
      - main
    paths-ignore:
      - '**.md'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: JavierSegoviaCordoba/actions/git/checkout@31dba23ba5922a4c61bfc31993ff6280c861ec22 # main

      - name: Set up Java
        uses: JavierSegoviaCordoba/actions/java/setup@31dba23ba5922a4c61bfc31993ff6280c861ec22 # main

      - name: Set up Gradle
        uses: JavierSegoviaCordoba/actions/gradle/setup@31dba23ba5922a4c61bfc31993ff6280c861ec22 # main

      - name: Build
        run: ./gradlew build -P root.project.name=javiersc-kotlin-template -P semver.stage=snapshot -P semver.scope=patch -P org.jetbrains.dokka.experimental.gradle.pluginMode=V2Enabled

      - name: Find Pull Request
        if: ${{ (github.event_name == 'pull_request') && (inputs.auto-approve == 'true' || inputs.auto-approve == true) }}
        id: find-pull-request
        uses: JavierSegoviaCordoba/actions/github/pull-request/find@31dba23ba5922a4c61bfc31993ff6280c861ec22 # main
        with:
          branch: 'renovate/hubdle'

      - name: Auto Approve
        if: ${{ (github.event_name == 'pull_request') && (inputs.auto-approve == 'true' || inputs.auto-approve == true) }}
        uses: JavierSegoviaCordoba/actions/github/pull-request/auto-approve@31dba23ba5922a4c61bfc31993ff6280c861ec22 # main
        with:
          github-token: '${{ secrets.TOKEN_GITHUB_ACTION }}'
          pull-request-number: ${{ steps.find-pull-request.outputs.number }}
